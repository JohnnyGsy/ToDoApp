<%= form_for(@project) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    
  <div class="input-group create_new_project">
    
    <%= f.text_field :name, class: 'form-control', placeholder: 'Start typing here to create a new project...', maxlength: 50, required: true %>
    <%= f.hidden_field :user_id, value: @user_id %>
    <span class="input-group-btn">
      <%= f.submit 'Create', remote: true, class: 'btn btn-success create_new_project_bt' %>
    </span>
  </div>
<% end %>
